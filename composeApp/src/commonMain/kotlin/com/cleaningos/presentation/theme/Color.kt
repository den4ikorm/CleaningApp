package com.cleaningos.presentation.theme

import androidx.compose.ui.graphics.Color

// ── Soft Liquid Glassmorphism — Dark Deep Ocean Palette ──────────────────────

/** #0F172A — Deep Dark Ocean Blue (app background) */
val OceanDeep        = Color(0xFF0F172A)

/** #1E293B — Translucent Frosted Glass (card surface) */
val FrostedGlass     = Color(0xFF1E293B)

/** #0D1829 — Deeper card variant */
val FrostedGlassDark = Color(0xFF0D1829)

/** #2DD4BF — Neon Mint-Cyan (primary accent, borders, glow) */
val CyanMint         = Color(0xFF2DD4BF)

/** #38BDF8 — Light Sky Blue (secondary accent) */
val SkyBlue          = Color(0xFF38BDF8)

/** #E0EEF9 — Primary text on dark */
val TextPrimary      = Color(0xFFE0EEF9)

/** #8DA8CA — Secondary / hint text */
val TextSecondary    = Color(0xFF8DA8CA)

/** #5A75A6 — Disabled / placeholder text */
val TextDisabled     = Color(0xFF5A75A6)

// ── Semantic Colors ───────────────────────────────────────────────────────────
val SemanticSuccess  = Color(0xFF14C47D)
val SemanticWarning  = Color(0xFFF9B80F)
val SemanticError    = Color(0xFFF04343)
val SemanticPurple   = Color(0xFF9C55F5)

// ── EDI Energy Scale ──────────────────────────────────────────────────────────
val EnergyLv1        = Color(0xFFF04343)   // exhausted
val EnergyLv2        = Color(0xFFF58520)   // tired
val EnergyLv3        = Color(0xFFF9D10F)   // neutral
val EnergyLv4        = Color(0xFF47C254)   // good
val EnergyLv5        = Color(0xFF2DD4BF)   // energized

// ── Glassmorphism Overlays ────────────────────────────────────────────────────
val GlowCyan10       = Color(0x1A2DD4BF)   // 10% cyan glow
val GlowCyan20       = Color(0x332DD4BF)   // 20% cyan glow
val GlowCyan35       = Color(0x592DD4BF)   // 35% cyan border
val CardOverlay      = Color(0x0DFFFFFF)   // frosted glass shimmer
